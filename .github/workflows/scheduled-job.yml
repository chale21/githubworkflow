name: Scheduled Automation Task

on:
  schedule:
    - cron: '*/5 * * * *'  # runs every 5 minutes
  workflow_dispatch:        # allows manual run

jobs:
  scheduled-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print scheduled message
        run: echo "Scheduled task is running"
